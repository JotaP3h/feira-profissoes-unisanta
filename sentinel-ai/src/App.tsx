import Index from "./pages/Index";

/**
 * Raiz do app. Atualmente renderiza apenas a landing page.
 * Em uma versão futura, este componente pode hospedar um router
 * (ex.: react-router) para múltiplas seções/páginas.
 */
export default function App() {
  return <Index />;
}
